    // -------- Deletes P content in the <main> -------- //
    // const main = document.querySelector('main');
    // const mainPTag = document.querySelectorAll('main p');
    // mainPTag.forEach(x => x.remove());
   // -------------------------------------------------- //